# Parser Windows Commands to Linux

Created to be more easier to use in SSH

---

## Instalation

Move all this folders to `C:\Program Files\Windows Parser Commands` and add each folder in `Path` in the `Environment Variables`.
